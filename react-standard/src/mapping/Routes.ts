export enum Routes {
    Root = "/",
    Login = "/login",
    Counter = "/counter",
    Calculator = "/calculator",
    LiveClock = "/live-clock",
    ToDoList = "/to-do-list",
    Board = "/board",
    Input = "/input",
    isLoggedIn = "isLoggedIn",
}
